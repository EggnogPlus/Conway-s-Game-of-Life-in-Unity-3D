using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Throw : MonoBehaviour
{
    // Update is called once per frame
    void Update()
    {
        //i thought i would need this for the throwing but adding it straight to the pickup file was easier... but now im scared to delete this ._.


    }
}
